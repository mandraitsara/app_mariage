{% extends 'base.html.twig' %}

{% block title %}Image
{% endblock %}

{% block body %}
	<h1>Image</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Identification :
				</th>
				<td>{{ image.id }}</td>
			</tr>
			<tr>
				<th>Nom de l'image :
				</th>
				<td>{{ image.name }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('app_image_index') }}">Liste des images
	</a>

	<a href="{{ path('app_image_edit', {'id': image.id}) }}">Modifier
	</a>

	{{ include('image/_delete_form.html.twig') }}
{% endblock %}
